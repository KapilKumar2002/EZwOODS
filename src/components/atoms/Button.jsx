import React from "react";
import { Link } from "react-router-dom";

const Button = () => {
  return (
    <Link className="font-buttonFont self-start text-lg px-7 py-3 rounded-full text-white bg-black flex justify-center items-center">
      Contact Us
    </Link>
  );
};

export default Button;
